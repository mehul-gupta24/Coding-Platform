# 5. Valid Parentheses
**Difficulty**: Easy

Given a string containing just the characters `'(', ')', '{', '}', '[' and ']'`, determine if the input string is valid.

### **Example**
Input: s = "()"  
Output: true  

Input: s = "(]"  
Output: false  

**Constraints**
- 1 <= s.length <= 10^4