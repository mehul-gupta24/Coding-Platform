# 2. Palindrome Number
**Difficulty**: Easy

Given an integer `x`, return `true` if `x` is a palindrome, and `false` otherwise.

### **Example**
Input: x = 121  
Output: true  

Input: x = -121  
Output: false  

### **Constraints**
- -2^31 <= x <= 2^31 - 1